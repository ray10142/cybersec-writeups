#!/bin/bash
# Run this once on your machine inside the cloned repo

git init
git add .
git commit -m "init: portfolio structure + README + write-up template"

# Replace YOUR_USERNAME with your GitHub username
echo ""
echo "Now run these two commands:"
echo "  git remote add origin https://github.com/YOUR_USERNAME/cybersec-writeups.git"
echo "  git push -u origin main"
